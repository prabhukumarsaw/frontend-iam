import type { UserType } from "@/types"

// userData has been removed in favor of real data from useAuthStore.
// If you need mock data for testing, please create a separate mock file.
export const userData: UserType | null = null
